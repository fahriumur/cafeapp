<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Restoranapp"
        tools:targetApi="31">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Restoranapp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dışa açık olan aktiviteler -->
        <activity android:name=".SiparisOnayActivity" android:exported="true" />
        <activity android:name=".SiparisVerActivity" android:exported="true" />
        <activity android:name=".SepetActivity" android:exported="true" />
        <activity android:name=".SiparisDurumuActivity" android:exported="true" />
        <activity android:name=".SiparislerimActivity" android:exported="true" />
        <activity android:name=".AdminLoginActivity" android:exported="true" />

        <!-- Dışa kapalı olan aktiviteler -->
        <activity android:name=".YoneticiPaneliActivity" android:exported="false" />
        <activity android:name=".SiparisOzetiActivity" android:exported="false" />
        <activity android:name=".SalatalarActivity" android:exported="false" />
        <activity android:name=".TatlilarActivity" android:exported="false" />
        <activity android:name=".AperatiflerActivity" android:exported="false" />
        <activity android:name=".AnaYemeklerActivity" android:exported="false" />
        <activity android:name=".IceceklerActivity" android:exported="false" />

    </application>
</manifest>
