<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Toplam Tutar TextView -->
    <TextView
        android:id="@+id/txt_toplam_tutar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Toplam Tutar: 0.0 TL"
        android:textSize="18sp"
        android:layout_gravity="center"
        android:paddingTop="16dp"
        android:paddingBottom="16dp" />

    <!-- Sepet içeriği -->
    <LinearLayout
        android:id="@+id/sepet_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" />

    <!-- Kullanıcı bilgilerini girmek için alanlar -->
    <EditText
        android:id="@+id/edt_ad_soyad"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Ad Soyad"
        android:padding="16dp"
        android:minHeight="48dp"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/edt_telefon"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Telefon"
        android:padding="16dp"
        android:minHeight="48dp"
        android:textSize="16sp" />

    <EditText
        android:id="@+id/edt_adres"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Adres"
        android:padding="16dp"
        android:minHeight="48dp"
        android:textSize="16sp" />

    <!-- Sipariş onaylama butonu -->
    <Button
        android:id="@+id/btn_siparisi_onayla"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Siparişi Onayla"
        android:layout_marginTop="16dp" />

</LinearLayout>
